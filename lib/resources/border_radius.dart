import 'package:flutter/material.dart';

const double extraSmallRadius = 5;
const double smallRadius = 8;
const double mediumRadius = 12;
const double largeRadius = 25;
const double extraLargeRadius = 50;
const double circularRadius = 1000;

final BorderRadius extraSmallBorderRadius = BorderRadius.circular(extraSmallRadius);
final BorderRadius smallBorderRadius = BorderRadius.circular(smallRadius);
final BorderRadius mediumBorderRadius = BorderRadius.circular(mediumRadius);
final BorderRadius largeBorderRadius = BorderRadius.circular(largeRadius);
final BorderRadius extraLargeBorderRadius = BorderRadius.circular(extraLargeRadius);
final BorderRadius circularBorderRadius = BorderRadius.circular(circularRadius);
